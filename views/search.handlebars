<div class="w-full h-100 bg-gray-400  lg:w-1/2 flex items-center justify-center"><!--LOGO left side-->
  <div class="flex mb-200 justify-center">
    <img src="./images/game chaser logo 3.0.png"
      alt="logo of a skeleton hand with a gaming controller in it with the words Game Chaser" class="w-3/4 h-3/4">
  </div>
</div>
<div class="w-full h-100 bg-gray-400 lg:w-1/2 flex items-center justify-center pb-10"><!--Form left side-->
  <div class="w-full p-6"><!--Login-->
    <h2 class="text-3xl font-semibold mb-6 text-black text-center">Find The Perfect Game For You!</h2>
    <form class="flex justify-center h-10 w-full">
      <label for="firstSelect" class="mr-5 font-bold">Search:</label>
      <select id="firstSelect" class="rounded-xl mr-5">
        <option value="" disabled selected>Select A Category</option>
        <option id="genre" value="genre">Genre</option>
        <option id="platform" value="platform">Platform</option>
        <option id="decade_created" value="decade_created">Decade Created</option>
        <option id="rating" value="rating">Rating</option>
      </select>
      <select id="secondSelect" class="rounded-xl mr-5 hidden">
        <!-- Options will be dynamically added here through js -->
      </select>
      <button id="subBtn" type="submit"
        class="bg-gray-700 hover:bg-black text-white font-bold py-2 px-4 border-b-4 border-blue-700 hover:border-pink-500 rounded">Submit</button>
    </form>
    <h1 class="text-sm font-semibold mt-8 text-gray-600 text-center">Results</h1>
<!-- Container for dynamically added game cards -->
<div class="display-games grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 p-4">
  <!-- Game cards will be dynamically appended here -->
</div>
  </div>
</div>
{{!-- <div class="flex w-full justify-center my-10">
  <h1 class="text-2xl font-bold flex justify-center">Find The Perfect Game For You!</h1>
  <form class="flex justify-center h-10 w-screen">
    <label for="firstSelect" class="mr-5 font-bold">Search:</label>
    <select id="firstSelect" class="rounded-xl mr-5">
      <option value="" disabled selected>Select A Category</option>
      <option id="genre" value="genre">Genre</option>
      <option id="platform" value="platform">Platform</option>
      <option id="decade_created" value="decade_created">Decade Created</option>
      <option id="rating" value="rating">Rating</option>
    </select>
    <select id="secondSelect" class="rounded-xl mr-5 hidden">
      <!-- Options will be dynamically added here through js -->
    </select>
    <button id="subBtn" type="submit"
      class="bg-gray-700 hover:bg-black text-white font-bold py-2 px-4 border-b-4 border-blue-700 hover:border-pink-500 rounded">Submit</button>
  </form>
</div>
<!-- Container for dynamically added game cards -->
<div class="display-games grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 p-4">
  <!-- Game cards will be dynamically appended here -->
</div> --}}